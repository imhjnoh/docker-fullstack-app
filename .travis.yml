language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t imhjnoh/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true imhjnoh/react-test-app npm run test

after_success:
  - docker build -t imhjnoh/docker-frontend ./frontend
  - docker build -t imhjnoh/docker-backend  ./backend
  - docker build -t imhjnoh/docker-nginx    ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push imhjnoh/docker-frontend
  - docker push imhjnoh/docker-backend
  - docker push imhjnoh/docker-nginx
